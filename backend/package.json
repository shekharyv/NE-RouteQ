{
  "name": "ne-routeiq-backend",
  "version": "1.0.0",
  "description": "NE-RouteIQ FastAPI Backend API Server",
  "main": "app/main.py",
  "scripts": {
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 5000",
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 5000 --reload"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
